name: ci

on:
  push:
  pull_request:

jobs:
  app-core-linux:
    runs-on: ubuntu-24.04
    container: swift:6.3.3-noble
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        run: swift format lint --strict --configuration .swift-format -r Packages HelloApp
      - name: Test
        working-directory: Packages/AppCore
        run: swift test
